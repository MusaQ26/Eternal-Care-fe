import React from 'react';

// This file used to provide token helpers. Token helpers have been moved to
// `utils/authStore.ts` (project root) to avoid being treated as a route by expo-router.
// We keep a tiny default export here so the router doesn't warn about a missing
// default export for this path.

export default function _AuthStorePlaceholder() {
  return null;
}
